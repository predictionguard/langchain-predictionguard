name: branch-build

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: Installing requirements, testing functions, and publishing to PyPI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@e0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        uses: astral-sh/setup-uv@03947b9bd8e9f986429fa0c5a41c367cd732b41 # 7.2.1

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run tests
        run: |
          uv run pytest tests -sxv
        env:
          PREDICTIONGUARD_API_KEY: ${{ secrets.PREDICTIONGUARD_API_KEY }}
          PREDICTIONGUARD_URL: ${{ vars.PREDICTIONGUARD_URL }}
          TEST_CHAT_MODEL: ${{ vars.TEST_CHAT_MODEL }}
          TEST_MULTIMODAL_EMBEDDINGS_MODEL: ${{ vars.TEST_MULTIMODAL_EMBEDDINGS_MODEL }}
          TEST_RERANK_MODEL: ${{ vars.TEST_RERANK_MODEL }}

      - name: Build package
        run: uv build

      - name: Publish to TestPyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.TESTPYPI_API_TOKEN }}
          UV_PUBLISH_URL: https://test.pypi.org/legacy/
        run: uv publish